/*
A pure function is a function where 
the output value is determined only by its input values, 
without observable side effects
*/

function addTwoNums(a, b) {
    console.log(a + b)
}

addTwoNums(5,6); // 11